# Tencent Cloud SDK for SSL service and DNSPod
tencentcloud-sdk-python>=3.0.1000

# DNSPod specific SDK (included in tencentcloud-sdk-python, listed for clarity)
# tencentcloud-sdk-python-dnspod>=3.0.900

# HTTP requests
requests>=2.31.0

# Cryptography for certificate handling
cryptography>=41.0.0

# OpenSSL bindings for certificate operations
pyOpenSSL>=23.0.0

# Environment variable loading
python-dotenv>=1.0.0

# ACME protocol implementation (Certbot official)
# Note: josepy is included as a dependency of acme
acme>=2.0.0
